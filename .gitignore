__pycache__/
*.py[cod]
.pytest_cache/
.venv/
dist/
build/
*.egg-info/
uv.lock
# the desktop app's npm + packaging surfaces (never the pip package's problem)
app/node_modules/
app/dist/
app/dist-electron/
app/release/
server/build/
# the sidecar bundle electron-builder consumes is a build artifact
app/release/sidecar/
